# Keep-the-Cluster-Alive-Detect-Reroute-Recover
An agent that monitors a simulated cluster of worker nodes handling a continuous stream of tasks. Nodes can independently become degraded or fully unavailable at unknown times, for unknown durations, and may or may not recover on their own.
